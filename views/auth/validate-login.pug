extends ../layouts/common.pug

block content
    h1.ml-5 Errors Login

    if errors
        each error in errors
            .alert.alert-danger.ml-5(style="width:400px")= error
    a#myButton.ml-5.btn.btn-primary(href="/auth/login") Back to Login

    script(src="./waitLogin.js")
